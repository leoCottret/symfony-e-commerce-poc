{% extends 'base.html.twig' %}

{% block title %}Se connecter - {{ brand_name }}{% endblock %}

{% block content %}

    <div class="block-small">
        {% for message in app.flashes('notice') %}
            <div class="alert alert-info">{{ message }}</div>
        {% endfor %}

        <form method="post">

            <h1 class="h3 mb-3 font-weight-normal">Réinitialiser mon mot de passe</h1>
            <label for="inputEmail">Email</label>
            <input type="email" name="email" id="inputEmail" class="form-control" autocomplete="email" required autofocus>

            <!-- TODO remove ugliness -->
            <button class="btn btn-lg btn-block mt-4 mb-2" type="submit">
                Valider
            </button>
            <hr>
            <p class="text-center">Ou souhaitez-vous <a href="{{ path('login') }}">vous connecter</a> ?</p>
        </form>
    </div>

{% endblock %}
